// BASE is the Vite base URL — '/' in dev, '/townwars/' in production builds.
// All API fetches and hub URLs are relative to this so the app works at any subpath.
export const BASE = import.meta.env.BASE_URL;
